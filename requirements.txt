requests>=2.28
beautifulsoup4>=4.12
feedparser>=6.0

# Enhanced price data sources
yfinance>=0.2.18
akshare>=1.9.0

# Optional: Alpha Vantage API for additional US stock data
# Get free API key at: https://www.alphavantage.co/support/#api-key

# Optional: `readability-lxml` improves article extraction but may be
# difficult to install on some Windows/Python setups. Install it only if
# you want stronger extraction:
#
#   python -m pip install readability-lxml

